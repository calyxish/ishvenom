# Mobile assets

Placeholder directory. Add real icon.png, splash.png, and adaptive-icon.png
before building with EAS. The Expo app.json references them.

Phase 1 ships without real assets — Expo will use defaults during `pnpm start`.
