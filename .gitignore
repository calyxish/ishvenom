# ── Node ──────────────────────────────────────────────
node_modules/
.npm
*.tsbuildinfo
*.tgz
.eslintcache
.stylelintcache

# ── Logs ──────────────────────────────────────────────
logs
*.log
npm-debug.log*
yarn-debug.log*
lerna-debug.log*
pnpm-debug.log*

# ── Environment ──────────────────────────────────────
.env
.env.*
!.env.example

# ── Data (large datasets live on HuggingFace / Kaggle) ──────────────
data/
# Exception: the small processed SFT corpus is worth keeping in git
!data/processed/
!data/processed/corpus/

# ── Next.js ──────────────────────────────────────────
.next
out

# ── Expo / React Native ─────────────────────────────
**/.expo/
**/.expo-shared/
**/android/
**/ios/
*.apk
*.aab
*.ipa

# ── Python ───────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.venv/
venv/
.venv-*/
venv-*/
.pytest_cache/
.mypy_cache/
.ruff_cache/
__pypackages__/
*.whl
.ipynb_checkpoints/

# ── ML model artifacts (live on HuggingFace, not git) ─
*.gguf
*.tflite
*.onnx
*.bin
*.safetensors
*.pt
*.pth
*.h5
apps/mobile/assets/models/*.gguf
apps/mobile/assets/models/*.tflite

# ── ML datasets (downloaded at runtime) ──────────────
ml/datasets/images/
ml/datasets/raw/
ml/datasets/splits/

# ── ML training run artifacts (checkpoints, logs — models live on HuggingFace) ─
runs/
ml/runs/

# ── Coverage ─────────────────────────────────────────
coverage/
*.lcov
.nyc_output/
htmlcov/
.coverage

# ── Claude Code (internal dev tooling, not needed in repo) ─
.claude/
CLAUDE.md

# ── Phase notes (internal dev logs, not for judges) ──
PHASE*_NOTES.md
lessons.md
AWARD_ANALYSIS.md

# ── Internal task tracking ────────────────────────────
tasks/

# ── Large data archives (source data lives on HuggingFace / Kaggle) ─
*.zip

# ── Editor backup files ───────────────────────────────
*.bak

# ── IDE ──────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# ── Build ────────────────────────────────────────────
dist/
build/Release

# ── pnpm ─────────────────────────────────────────────
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases

# ── Misc ─────────────────────────────────────────────
*.pid
*.seed
*.pid.lock
.cache/
.parcel-cache/
.serverless/
.dynamodb/
.firebase/
.fusebox/
.tern-port
.venv-py39/